This directory contains analysis scripts.
Note that Scripts `05_Random_effects_model.R` and `06_Correlation_analysis.R` need to be run after Scripts `01`–`04` within the same R session, as they rely on objects created in earlier scripts.
